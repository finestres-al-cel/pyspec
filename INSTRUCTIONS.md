
Usage instructions:
1. Open a new terminal
2. Go to planet_orbits `bin` folder:
    `cd <path to your pyspec repo>/bin`
2. Load your conda environment (see README.md):
    `conda activate my_pyspec_env`
3. Launch `pyspec`:
    `python pyspec_app.py`